%-------------------------
% Pankaj Kumar — Resume (Software Development track)
%-------------------------
\documentclass[letterpaper,10.9pt]{article}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{helvet}
\renewcommand{\familydefault}{\sfdefault}

\usepackage[top=0.38in, bottom=0.38in, left=0.45in, right=0.45in]{geometry}

\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{enumitem}
\usepackage{hyperref}
\usepackage{array}
\usepackage{tabularx}
\pagestyle{empty}

\definecolor{darkblue}{HTML}{1a1a2e}
\definecolor{accent}{HTML}{2E75B6}
\definecolor{darkgray}{HTML}{555555}
\definecolor{lightgray}{HTML}{888888}

\hypersetup{colorlinks=true, urlcolor=accent, hidelinks}

% Section: bold label + blue rule beneath
\titleformat{\section}{\bfseries\normalsize\color{darkblue}}{}{0em}{}[{\color{accent}\vspace{1pt}\hrule height 0.6pt\vspace{2pt}}]
\titlespacing*{\section}{0pt}{8pt}{4pt}

\setlength{\parindent}{0pt}
\setlength{\parskip}{0pt}

\setlist[itemize]{
leftmargin=1.2em,
itemsep=2pt,
topsep=2pt,
parsep=0pt,
partopsep=0pt,
label=\textbullet
}

\newcommand{\resumeItem}[1]{\item\small #1}

% Fixed: title (flexible column) + links (right-aligned column) on one row
\newcommand{\projectHeading}[4]{%
\noindent\begin{tabularx}{\linewidth}{@{}X r@{}}
\textbf{\color{darkblue}#1} & {\small\href{#2}{\color{accent}\underline{Live Demo}}\;\color{lightgray}{|}\;\href{#3}{\color{accent}\underline{GitHub}}} \\
\end{tabularx}\\[-2pt]
{\footnotesize\color{lightgray}\textit{#4}}%
}

\newcommand{\jobHeading}[3]{%
  \textbf{\color{darkblue}#1} {\color{darkgray}--- #2}%
  \hfill{\footnotesize\color{lightgray}\textit{#3}}%
}

\newcommand{\skillrow}[2]{\small\textbf{\color{darkblue}#1:} #2}

%=============================================================
\begin{document}
%=============================================================

%-----Header-------------------------------------------
\begin{center}
{\fontsize{24}{27}\selectfont\bfseries\color{darkblue} (( personal_info.name ))}\\[4pt]

{\small\color{darkgray}
(( personal_info.email )) \;|\;
(( personal_info.phone )) \;|\;
(( personal_info.location )) \;|\;
\href{(( personal_info.linkedin ))}{LinkedIn}
\;|\;
\href{(( personal_info.github ))}{GitHub}
\;|\;
\href{(( personal_info.leetcode ))}{LeetCode}
}
\end{center}

\vspace{2pt}

% ── Profile Summary ─────────────────────────────────────────────
((* if profile_summary *))
\section{Profile Summary}
{\small\color{darkgray} (( profile_summary ))}
((* endif *))

% ── Education ────────────────────────────────────────────────
\section{Education}
((* for edu in education *))
\textbf{\color{darkblue}(( edu.institution ))} --- \textit{(( edu.degree ))} %
\hfill{\footnotesize\color{lightgray}\textit{(( edu.duration ))}}\\[1pt]
{\small\color{darkgray}(( edu.details ))}
((* endfor *))

% ── Achievements ─────────────────────────────────────────────
((* if achievements *))
\section{Achievements}
\begin{itemize}
((* for ach in achievements *))
\resumeItem{(( ach ))}
((* endfor *))
\end{itemize}
((* endif *))

% ── Experience ───────────────────────────────────────────────
((* if experience *))
\section{Experience}
((* for exp in experience *))
\jobHeading{(( exp.role ))}{(( exp.company ))}{(( exp.duration ))((* if exp.location *)) · (( exp.location ))((* endif *))}
\begin{itemize}
((* for bullet in exp.bullets *))
\resumeItem{(( bullet ))}
((* endfor *))
\end{itemize}
((* endfor *))
((* endif *))

% ── Projects ─────────────────────────────────────────────────
\section{Projects}
((* for proj in projects *))
\projectHeading%
  {(( proj.name ))}%
  {(( proj.links.demo if proj.links and proj.links.demo else '#' ))}{(( proj.links.repo if proj.links and proj.links.repo else '#' ))}%
  {(( proj.stack ))}
\begin{itemize}
((* for bullet in proj.bullets *))
\resumeItem{(( bullet ))}
((* endfor *))
\end{itemize}
\vspace{4pt}
((* endfor *))

% ── Technical Skills ─────────────────────────────────────────
\section{Technical Skills}
((* if skills.languages *))\skillrow{Languages}{(( skills.languages | join(', ') ))}\\[3pt]((* endif *))
((* if skills.core_cs *))\skillrow{Core CS}{(( skills.core_cs | join(', ') ))}\\[3pt]((* endif *))
((* if skills.frameworks *))\skillrow{Frameworks \& Libraries}{(( skills.frameworks | join(', ') ))}\\[3pt]((* endif *))
((* if skills.backend_databases *))\skillrow{Backend \& Databases}{(( skills.backend_databases | join(', ') ))}\\[3pt]((* endif *))
((* if skills.machine_learning *))\skillrow{Machine Learning}{(( skills.machine_learning | join(', ') ))}\\[3pt]((* endif *))
((* if skills.tools *))\skillrow{Tools}{(( skills.tools | join(', ') ))}\\[3pt]((* endif *))

% ── Certifications ───────────────────────────────────────────
((* if certifications *))
\section{Certifications}
((* for cert in certifications *))
((* if cert is mapping *))
\textbf{\color{darkblue}(( cert.title )) --- } {\small\href{(( cert.badge_url ))}{\color{accent}\small{Credly Verified Badge}}}\\[2pt]
{\small (( cert.details ))}
((* else *))
\resumeItem{(( cert ))}
((* endif *))
((* endfor *))
((* endif *))

\end{document}
